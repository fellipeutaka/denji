# Denji

CLI tool for managing SVG icons in frontend projects — fetches from Iconify, converts to framework-specific components (React, Preact, Solid, Vue, Svelte, Qwik).

## Docs Lookup

When implementing or fixing bugs involving external libraries, always consult Context7 for up-to-date documentation before writing code. Never rely on training data for library APIs.

## Plan Mode

- Make the plan extremely concise. Sacrifice grammar for the sake of concision.
- At the end of each plan, give me a list of unresolved questions to answer, if any. Make the questions extremely concise.

## Dependencies

Check `package.json` root and `workspaces.catalog` before adding deps.
Use `bun add` to install.
