---
"denji": minor
---

Add Vue framework support

Implements Vue strategy using `h()` render functions for icon components. Generates `FunctionalComponent`-typed icons with `SVGAttributes` props.

Closes #8
